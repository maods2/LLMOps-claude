global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'llm-serving'
    static_configs:
      - targets: ['serving:8090', 'serving-dev:8090']

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
